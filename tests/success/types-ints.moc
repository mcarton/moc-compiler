void main() {
    Int v;
    Int w;
    Int x;
    Int y;
    Int z;

    // Some random expressions
    1*8*z*2 + 8*v + 1*6*z - 6 + 1*5*w*5 + 3*y*4;
    9 - 1*9*z - 9*x - 5*y * 1*8*w / (1*6*y*9);
    +2*0*(-w);
    7*z - 3 + 1*3;
    -1*6*v - 2*3*x - 3 * 1*8*y * 1*z / (1*3*v / (8*w));
    1*2 / (2);
    5*y + 2*4*v + 8*v*9 * 2*3*x + 4*v * 7*v + 8*y + 2*5*y;
    1*4 / (9*x * 2*x*7 - 1*4*y - 1*6*x);
    6*y;
    6*w + 3*v * 8*z * 1*8*x * 6 + 1;
    -2*5 + 2*0*z * 1*7*v - 1*5;
    1*5*v - 1*9*z*3 * 4*(+y)*8 + 2*v*1*0 - 1*6;
    4*z / (2*2*v) + 5*w*2 + 1*2*w - 2*3*z*8 - 2*3*z * 1*x*3;
    1*7 + 9*z*9 + 2*5*w + 2*2*z*2;
    1*5 + 2*3*w*4 * 3;
    -2*2*y*2 - 7*(+v) + 7*v + 4*y + 1*3*(-x) - 8*v*2 + 1*2*w + 1*1*y*5;
    8*y*3 + 2*2;
    2*1*x;
    1*9*w;
    1*2 - 1*6*w * 1*(+2);

    0 && 42;
    42 && 0;
    42 && 43;
    0 || 42;
    42 || 0;
    42 || 43;

    !(42 == 43);
    !(42 != 42);
}
